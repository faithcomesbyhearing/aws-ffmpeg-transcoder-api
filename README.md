# AWS ffmpeg Transcoder API

- API Gateway
- Step Functions
- Lambda

## Deployment

The project uses AWS CodeBuild to compile, zip, and update each Lambda function’s code”.
Afterwards, the user must manually access the AWS console to publish a new version for each Lambda.
